/** Compatibility facade. Canonical ownership lives in @ai-agent-sdk/protocol-responses. */
export {
  serializeResponsesRequest,
  type ResponsesReasoningState,
} from '@ai-agent-sdk/protocol-responses'
