# Changesets

All publishable packages remain private during structural migration. They start in one fixed `0.1.x` release group; release preflight removes `private` only after npm scope ownership is proven.
